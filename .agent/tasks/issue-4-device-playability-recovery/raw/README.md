# Sanitized receipts

Raw device identifiers, signing identities, and local logs are excluded from
this public repository.

The triggering receipt is the owner's fourth physical iPhone rejection of
`989dee5`.

Sanitized local receipts retained outside the repository cover:

- a red domain assertion where correct tilt left full gust acceleration;
- a red delayed-input matrix where Wild with five creatures collapsed;
- final `12/12` EditMode and `13/13` PlayMode XML results;
- web `11/11` tests and production build;
- Mac smoke build plus an inspected 1179 × 2556 Metal gameplay capture;
- non-Development iOS export, signed arm64 Xcode build, strict signature
  verification, paired-device install, and paired-device launch.
